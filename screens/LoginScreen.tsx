import React from "react";
import { FormLoginScreen } from "../components/form-login-screen";

const LoginScreen = ({ navigation }: any) => {
  return (
    <>
      <FormLoginScreen navigation={navigation} />
    </>
  );
};

export default LoginScreen;
